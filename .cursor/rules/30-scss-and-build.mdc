---
description: Sass 与构建约定（全局样式、变量注入、Vite base）
globs: src/**/*.{scss,vue} vite.config.ts
alwaysApply: false
---

# Sass / 构建约定

- **BEM 命名**：块 `{domain}-{name}`、元素 `{block}__{part}`、修饰符 `{block}--{variant}`；统一 kebab-case。
- **样式组织**：全局入口 `src/assets/main.scss`；partial 放 `src/assets/styles/`（如 `_layout.scss`、`_components.scss`）。
- **变量注入机制**：Vite 会对“非样式目录内的 scss”自动注入 `@use "variables" as *;`（见 `vite.config.ts`）。
  - 在 `src/assets/styles/**` 或 `src/assets/main.scss` 内 **不要** 再额外注入/重复 `@use variables`，避免重复 `@use` 报错。
- **部署 base**：生产构建 `base` 为 `/frontend-interview-vue/`，开发为 `/`；写路由/资源路径时使用 `import.meta.env.BASE_URL` 思维，避免硬编码根路径。

